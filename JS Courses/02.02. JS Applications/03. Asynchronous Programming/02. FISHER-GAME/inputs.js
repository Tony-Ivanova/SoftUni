const addButton = document.querySelector("button.add");
const loadButton = document.querySelector("button.load");
const catchesDiv = document.getElementById('catches');
const angler = document.querySelector('fieldset > input.angler');
const weight = document.querySelector('fieldset > input.weight');
const species = document.querySelector('fieldset > input.species');
const location = document.querySelector('fieldset > input.location');
const bait = document.querySelector('fieldset > input.bait');
const captureTime = document.querySelector('fieldset > input.captureTime');


export {addButton, loadButton, catchesDiv, angler, weight, species, location, bait, captureTime}